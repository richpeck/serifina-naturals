%ul
  - @products.each do |product|
    %li
      %a{href: "https://\#{@shop.myshopify_domain}/admin/products/\#{product.id}", target: :blank}= "#{product.name} (#{product.shape_type.titelize})"

= @products.size
