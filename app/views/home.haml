%ul
  - @products.each do |product|
    %li
      %a{href: "https://\#{@shop.myshopify_domain}/admin/products/\#{product.id}", target: :blank}= "#{product.name.upcase} (#{product.shape_type.capitalize})"

= "#{@products.size} Shapes"

- Charm.all.each do |charm|
  = charm.charm_type
  = charm.name
  <br />
